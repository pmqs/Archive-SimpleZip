
use lib 't';

use Test;

use File::Temp;;
use ZipTest;

#my $base_dir = tempdir(:unlink(True));
#say $base_dir;
#my ($fname,$fh) = tempfile(:tempdir($base_dir));
#say $fname ;
#say $fh ;


say "done";
